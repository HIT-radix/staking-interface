import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const loadings = createSlice({
  name: "loadings",
  initialState,
  reducers: {},
});

export default loadings.reducer;

export const {} = loadings.actions;
